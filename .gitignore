# Runtime memory and seed artifacts — regenerated per /magic run
memory/*
!memory/.gitkeep
seeds/draft-*.html
seeds/starter.html
seeds/tokens.css
!seeds/.gitkeep
!seeds/README.md

# macOS
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Local test runs / sample outputs (not shipped)
magic-sample/

# graphify (deprecated, stripped due to cost — see /fullsave step 5 removal)
graphify-out/
